apiVersion: v1
kind: Secret
metadata:
  name: flask-secret
type: Opaque
data:
  SECRET_KEY: <base64-encoded-value>
  # Generate your own: python3 -c "import secrets, base64; print(base64.b64encode(secrets.token_hex(32).encode()).decode())"
